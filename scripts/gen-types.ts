/**
 * Placeholder for TypeScript type generation from the live Supabase schema.
 * Once the database exists, generate types (via the Supabase MCP `generate_typescript_types`
 * or `supabase gen types typescript`) into src/lib/db/database.types.ts, then have the
 * repository layer import them. Kept out of version control (.gitignore) as generated output.
 */
console.log(
  'Run the Supabase type generator against the ship-faster project and write to src/lib/db/database.types.ts',
);
